extends layout

block content
  h1= title
  a(href='/create') Add Item
  br
  br
  input(type='text' placeholder='Search' oninput='myFunction()')
  select(name='filter' onchange='myFunction()')
    option(value='Room') Room
    option(value='Container') Container
    option(value='Content') Content
  br
  br
  p #{item_count} Items
  table 
    tr 
      th Room
      th Container 
      th Content
    each item in item_list
      tr
        td= item.room
        td= item.container
        td= item.content
        td
          a(href=item._id + '/update') Edit
        td
          a(href=item._id + '/delete') Delete
  a(href='/log-out') Log Out
